[pytest]
testpaths = tests
# Keep legacy/ archived tests out of default collection; they import the old
# `redteam` package which no longer lives on sys.path.
norecursedirs = legacy outputs assets paper notebooks .git
